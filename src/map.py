#this displays the map and where the hero is in the world and potentially where everyone else is
class map:
	def __init__(self):
		self.rect.x = x
		self.rect.y = y
		
	def playerLocation(self):
	
	def enemyLocation(self):
		
